/**
 * ENSv2 writer/resolver — lands with the Sepolia research (commit-reveal
 * registrar + text-record resolver). Placeholder module so the package
 * compiles; the register/resolve scripts import from here.
 */

export {};
