{
  "name": "@repayd/engine",
  "version": "0.1.0",
  "description": "REPAYD Watcher & Verdict Engine — deterministic tier logic, anomaly scoring, alibi check. The AI narrates; the code decides.",
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
