{
  "name": "@bulwark/api",
  "version": "0.1.0",
  "description": "BULWARK Coverage API — embedded insurance for agent platforms. POST /v1/coverage.",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@bulwark/engine": "workspace:*",
    "@bulwark/agent-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
