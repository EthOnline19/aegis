{
  "name": "@bulwark/demo",
  "version": "0.1.0",
  "description": "BULWARK demo — Atlas, the attack that never settles, and the same-block payout. Live on anvil.",
  "type": "module",
  "scripts": {
    "demo": "bun run src/demo.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@bulwark/engine": "workspace:*",
    "@bulwark/agent-sdk": "workspace:*",
    "viem": "^2.21.0",
    "@noble/curves": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
