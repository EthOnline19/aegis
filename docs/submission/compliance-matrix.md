# REPAYD — ETHOnline 2026 Compliance Matrix

One table per track. **Every** qualification requirement and extra-scoring criterion from the official prize pages ([ethglobal.com/events/ethonline2026/prizes](https://ethglobal.com/events/ethonline2026/prizes)) is a row, verbatim. Evidence cells are grounded in real, independently-verified artifacts — the 14 Step-4 Arc-testnet transaction hashes (all re-fetched, status 0x1, see [`local/step4-final-report.md`](../../local/step4-final-report.md) and `.wsl/step4-receipt.json`), ERC-8004 registry readbacks (agentId **894341**, validation score **25 = COVERED**, feedback **−2500 @ 2dp**, verdict digest `0x1af03bc6a70b6309bd5c9ec92c7d78c1024e0d69c9ea5ea60faf828c958ed3f0`), the fresh-stack addresses in [`contracts/deployments/5042002.json`](../../contracts/deployments/5042002.json), and git commits. **No evidence is invented; every cred-gated item is marked.**

Status legend: **DONE** (verifiable now, in-repo or on-chain) · **IN-PROGRESS** (code exists, integration under construction) · **BLOCKED-ON-CRED** (work complete, credential needed to execute/record) · **NEEDS DECISION** (human call required).

---

## Track 1 — Arc: "Best Agentic Economy Application with Circle Agent Stack" ($3,500; $2,500 of the pool requires Arc **mainnet** deploy by **Sept 30, 2026**)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| A1 | "agents with clear decision logic tied to real signals" | GuardAccount's `_classify` computes every tier deterministically from **on-chain signals**: allowlist membership, recipient first-seen block, UTC hour, per-tx/daily caps, velocity. No LLM in any decision path. `contracts/src/GuardAccount.sol`; engine mirror `packages/engine/src/engine.ts` (27 tests incl. same-inputs-same-verdict). Live proof: the $900 → ATTACKER payment tripped `TIER_VIOLATION/OVER_PER_TX` and was **blocked pre-broadcast** (no tx hash by design — ATTACKER balance 0, confirmed in report §3); the $150 → 3-day-old FRESH_WALLET tripped `NEW_RECIPIENT` → hold (tx `0xf795bf3f813248f4fd463adb91af6ca12c4208a51137de423f5b4d5e440934a1`). | Live chain txs + contracts + tests | **DONE** |
| A2 | "autonomous spending/payments/settlement in USDC" | The REPAYD guard agent **autonomously executed four USDC payroll payments** on Arc testnet from its own agent key (`0x5c53855E30CfbB4e4a6341Bba6129a73cb450F9f`): $180→ALICE (`0xecd4321f…`), $310→BOB (`0x84beb66a…`), $90→ALICE (`0x9aff8383…`), $220→BOB (`0x52df9932…`) — all to guard `0xB30553e2f132126B951D3a6AD4E07EbAa5523b6E`, USDC `0xb95fe4d7EEDb98693ded8F3c1a34A59F6Dac114B`. Autonomous **settlement**: covered-claim payout $135 (135000000 units) to the policy owner in the **same block as the verdict** (tx `0xed020f97235164333e416ae494c6b24462a6884e1aa2ba1a1898b85d07a299f6`, `VerdictAccepted(digest 0x1af0…d3f0, payout=135000000)`; Amara balance = exactly 135000000, was 0 before run). | Live chain txs (Arcscan-linked in report §1) | **DONE** |
| A3 | "use of Agent Stack to connect agents to wallets, USDC payments, onchain actions" | **Implemented (gated, dry-run verified)**: `packages/api/src/circle/agent-wallet.ts` — a Circle **Developer-Controlled Wallet on `ARC-TESTNET`** as the REPAYD agent's treasury mirror: creates/reuses the wallet via `@circle-fin/developer-controlled-wallets`, reads its USDC balance via `listBalances` side-by-side with the GuardAccount's live on-chain balance, supports one gated USDC transfer (`CIRCLE_DEMO_SPEND=1`); dry-run default prints the exact API calls (zero network traffic). Full plan + ranked alternatives (Nanopayments Gateway path, rejected paymaster/App-Kit paths with reasons): [`circle-agent-stack.md`](circle-agent-stack.md) §3. Agent↔wallet binding also exists on-chain: `identity.getAgentWallet(894341)` = guard-owner wallet `0x9675b4D20d2ACFE55D00a02D55B9cdb57AEbD482` (registry readback, report §2); onchain actions via `packages/api/src/erc8004-orchestrator.ts` (commit `db98c7f`). Live Circle API calls **BLOCKED-ON-CRED** `CIRCLE_API_KEY`+`CIRCLE_ENTITY_SECRET` (checklist C3). Exposed at `GET /v1/circle/agent-wallet`; rendered on the dashboard owner page. | Implementation + registry readback + plan | **DONE** (gated) / live calls BLOCKED-ON-CRED (C3) |
| A4 | "Nanopayments/Paymaster/App Kits where relevant" | Nanopayments: per-2s-block pricing (`packages/pricing`, commit `981e977`) charges per active block — the metering primitive that maps onto Circle's Nanopayments Gateway (sub-cent USDC x402 payments; Path B in the Circle plan: deposit USDC into Gateway, pay REPAYD's own x402 endpoint — highest story value, next after Path A). Paymaster: Arc has **no hosted Circle paymaster API** — sponsorship comes gas-sponsored with Agent Wallets; operating our own ERC-4337 paymaster was evaluated and **deliberately rejected** (plan §3 Path C, risk/reward). App Kits: the three dashboards (`packages/dashboard/src/` `owner.html`/`capital.html`/`record.html`) are the MVP surfaces; App Kit embed evaluated and rejected as duplicative at API level (plan §3 Path D). Rationale documented = "where relevant" answered explicitly. | Code + plan §3 | **DONE** (rationale + metering) / live Nanopayments BLOCKED-ON-CRED (C3) |
| A5 | Gate 1: "Functional MVP: working frontend AND backend plus an architecture diagram" | Backend: Coverage API `packages/api/src/server.ts` (12 tests + live smoke, commits `ad325a7`/`56d5ee5` — store-first coverage quotes, config-gated on-chain PolicyRegistry bridge). Frontend: dashboards at `/`, `/capital`, `/record` (content-verified + API-integrated). Architecture diagram: `docs/submission/architecture.md` (sibling deliverable; README §Architecture has the ASCII original). | Code + tests + doc | **DONE** (diagram doc: IN-PROGRESS) |
| A6 | Gate 2: "Video demonstration + presentation: succinctly outlining core functions and effective use of Circle's Developer tools, supported by detailed documentation" | Script ready to record: `docs/submission/video-scripts.md` §ARC — second-by-second beats over the **real** Step-4 run (14 Arcscan tx links), Circle Agent Stack touchpoints at beats ARC-2/ARC-7. Documentation: this matrix + `docs/submission/circle-agent-stack.md`. Recording requires screen-capture only (no new creds). | Script + on-chain evidence | **DONE** (script) / recording pending |
| A7 | Gate 3: "GitHub repo link" | This repo (README rewritten as submission README with track claims + quickstart). | Repo | **DONE** |
| A8 | "State the bounty in the submission" | README headline + this matrix title state the Arc bounty explicitly. | README | **DONE** |
| A9 | ⚑ **RIDER: "$2,500 requires Arc MAINNET deploy by Sept 30"** ($2,500 base + $2,500 mainnet rider at stake) | Contracts are chain-agnostic and deploy via `contracts/script/Deploy.s.sol` (demo-ready, commit `514f5eb`) with per-chain `deployments/<chainId>.json`. Mainnet deploy needs: Arc mainnet RPC + funded deployer + Circle mainnet USDC addresses. **NEEDS DECISION**: cost/risk of a mainnet deployment vs. testnet-only submission. If pursued, the same two-gasp demo re-runs verbatim against mainnet stack. | Deploy script + decision | **NEEDS DECISION** (see credential checklist C4) |

---

## Track 2a — The Graph: "Best AI Tooling or AI Use Case (From Scratch)" ($5,000)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| G1 | "The Graph load-bearing (Subgraphs/Subgraph MCP/Substreams as source of blockchain data)" | The **REPAYD Risk Subgraph** (`packages/subgraph`) is the *single shared read replica* for the whole protocol: the pricing engine and résumé builder both consume it instead of running indexers (`packages/subgraph/README.md` §36 story; `packages/pricing` + `packages/record` read `Agent`/`Verdict`/`Claim`/`BlocklistEntry` entities via `PricingEvent` projection). 36 events across all six fresh-stack contracts, 9 core entities, mappings commit `1ec7979`, schema/manifest commit `bc5a400`, codegen+build verified (commit `4ef64e3`). | Code + build | **DONE** |
| G2 | "consume LIVE data from a Graph provider (Subgraph Studio API key; 'Mocked, local-only, or static datasets do not qualify')" | **LIVE** — both subgraphs deployed to Graph Studio and serving real indexed data: Risk Subgraph `https://api.studio.thegraph.com/query/1760165/repayd-risk-arc/v0.1.3` (v0.1.3, build `QmYBgJBw…h1i`; agents/claims/policies entities queried with live values: guard 0xb305…3b6e streak + totalRoutineTx 4, claim `0x1af0…d3f0` payout 135000000 covered=true) and ERC-8004 registries subgraph `https://api.studio.thegraph.com/query/1760165/repayd/v0.0.7` (v0.0.7, build `QmSuRqRZ…Emy6`; canonical registrations/feedback already queryable, syncing toward the Step-4 agentId 894341). No mocked, local-only, or static data anywhere in the consumer chain. Procedure + evidence: [`docs/submission/graph-deploy.md`](graph-deploy.md). | Live endpoint + captured JSON | **DONE — LIVE** |
| G3 | "meaningful work with the data (reasoning/decisions/automation/NL interface, not printing raw results)" | The AI/automation consumers: (a) **pricing engine** reasons over subgraph state — streak/claim/anomaly multipliers per §12 (commits `a0b85eb`, `5282591`, `9813b47`); (b) **live risk-posture consumer** `packages/api/scripts/query-graph.ts` — EXECUTED LIVE against the deployed endpoint: agent 0xb305…3b6e → multiplier **2.7×**, monthly premium **135 USDC**, labeled reasons (CLAIM_LOAD ×3 from paid claim ts 1789160834; SDK_DISCOUNT −10%), deterministic NL summary, anomaly flag POST_CLAIM_NO_STREAK (capture in graph-deploy.md §LIVE evidence); (c) résumé builder renders INSURED/DRIVING/CLAIMS/ALIBI/BACKING/STATUS with VERIFIED/COMPUTED provenance labels (commit `3212e7d`). | Live run output + tests | **DONE — LIVE** |
| G4 | "open-source + README" | Repo + this README + `packages/subgraph/README.md`. | Repo | **DONE** |
| G5 | "2-4 min video" | `docs/submission/video-scripts.md` §GRAPH (3:30 target): deployed-subgraph live query → AI consumer computes live Risk Posture → pricing decision from the same data. | Script | **DONE** (script) / recording pending G2 |
| G6 | "Start Fresh pool" (we are NOT continuity) | First-time ETHOnline project; subgraph authored from scratch this hackathon (commits `bc5a400`→`4ef64e3`, this session). | Git history | **DONE** |

## Track 2b — The Graph: "Best Use of Composable or Standardized Graph Products" ($5,000)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| S1 | "compose 2+ Graph products OR build meaningfully on a standardized schema (e.g. Messari Standardized Subgraphs)" | **Both paths LIVE**: (a) compose 2+ subgraphs — the bespoke Risk Subgraph (`repayd-risk-arc` v0.1.3) + an **ERC-8004/Agent0-standardized registry subgraph** (`repayd` v0.0.7) indexing the canonical identity/validation/reputation registries, joined cross-query on `agentId` (composition query in video script GRAPH-8; both endpoints live and queried this session); (b) standardized schema — REPAYD's verdict mirroring maps onto the ERC-8004 standard registries (identity `0x8004A818BFB912233c491871b3d84c89A494BD9e`, validation `0x8004Cb1BF31DAf7788923b405b754f57acEB4272`, reputation `0x8004B663056A597Dffe9eCcC1965A193B7388713`) — the exact schema Agent0 standardizes. The Step-4 run already posted live, readback-verified records to all three (agentId 894341; score 25; feedback −2500@2dp, tags `bulwark-verdict`/`covered`). | Two live endpoints + registry readbacks | **DONE — LIVE** |
| S2 | "live provider data" | Both halves LIVE on Studio: `repayd-risk-arc` v0.1.3 and `repayd` (erc8004 manifest) v0.0.7. The Step-4 ERC-8004 registry posts (txs `0x76973a7e…`, `0x0b84127d…`, `0x76fe46f2…`) are on Arc testnet and are indexed as the erc8004 subgraph syncs (startBlocks 29241340/29241344/29241349; canonical-registry data from other agents already queryable at block 31.4M+). **COST: $0** — Studio free tier. | Live endpoints + indexed data | **DONE — LIVE** |
| S4 | "CONTRIBUTING a standardized subgraph counts as in-scope" | The ERC-8004 registry subgraph (S1a) is LIVE on Studio (`repayd` v0.0.7) and written to be contributed back to the Agent0 ecosystem post-hackathon; the Risk Subgraph README documents the public-good intent (§36 story). | Code + live deployment | **DONE** (live; contribution PR is post-event) |

---

## Track 3 — Hedera: "AI & Agentic Payments on Hedera" ($6,000, up to 3 × $2,000)

| # | Requirement (verbatim) | Where we satisfy it | Evidence type | Status |
|---|------------------------|---------------------|---------------|--------|
| H1 | "host a LIVE x402-gated service on Hedera testnet settled through the Blocky402 facilitator" | `packages/hedera` — the REPAYD Coverage & Risk API with three pay-per-call endpoints behind an x402 `exact`/`hedera:testnet` paywall, verification + settlement delegated to the hosted Blocky402 testnet facilitator `https://api.testnet.blocky402.com` (open access, no key). Full design: [`hedera-x402-plan.md`](hedera-x402-plan.md) §1–§3 (payment flow, endpoint pricing table, architecture diagram). **BLOCKED on Hedera testnet operator/service accounts** to go live. | Plan + package scaffold | **BLOCKED-ON-CRED** (checklist C2) |
| H2 | "build a platform/agent that consumes it and completes at least one REAL paid request end-to-end" | `packages/hedera/scripts/paid-request.ts` — the REPAYD payer agent: discovers the service via the free `/v1/x402/services` directory, pays the 402 challenge through Blocky402 (payer-signed `TransferTransaction`, facilitator as fee-payer), receives the 200 payload, prints the settlement tx id + HashScan link. Dry-run (no keys) prints the full flow with real numbers; live run gated on C2. Video beat HEDERA-4/5 shows exactly this. | Plan §3 + script + video script | **BLOCKED-ON-CRED** (C2) |
| H3 | "public repo + README (setup/architecture/payment flow)" | This README + [`hedera-x402-plan.md`](hedera-x402-plan.md) (setup: portal.hedera.com 2× ECDSA accounts + faucet; architecture diagram; §2 payment flow: 402 → requirements → X-PAYMENT retry → facilitator /verify → 200 → /settle) + `packages/hedera` package docs (service usage + env vars; package-level README planned in plan §6). | Docs | **DONE** |
| H4 | "demo video ≤5 min showing the paid request executing" | `docs/submission/video-scripts.md` §HEDERA (4:30 target) — 402→payment→200 loop live, HashScan on the real settlement tx, HCS receipt. | Script | **DONE** (script) / recording pending C2 |
| H5 | Extra: "pay-per-call inference/data/compute metering" | ✅ Per-endpoint pricing table in the service (`src/pricing-meter.ts`, plan §3): quote 0.001 HBAR, verdict record 0.002 HBAR, résumé 0.0015 HBAR — every 402 challenge carries the endpoint's exact tinybar price, `/v1/x402/services` advertises prices up front, served calls counted per endpoint (exposed on `/healthz`). Mirrors the per-block pricing philosophy of `packages/pricing`. | Plan §3–§4 + code | **IN-PROGRESS** (with H1) |
| H6 | Extra: "on-chain agent identity via ERC-8004 or HCS-14" | **Already live on Arc**: ERC-8004 agentId **894341** with owner + wallet binding readback-verified (report §2). Hedera side: the payer agent identifies as agentId 894341 and the service cross-validates via read-only `ownerOf`/`getAgentWallet` eth_calls against the canonical identity registry `0x8004A818BFB912233c491871b3d84c89A494BD9e` when `ARC_RPC_URL` is set; the id is stamped into the HCS audit memo — identity on Arc, payments on Hedera, one cross-chain story (plan §4). | Registry readback + plan §4 | **DONE** (Arc side) / HCS stamp with H1 |
| H7 | Extra: "agent discovery via UCP or a directory" | `GET /v1/x402/services` — a JSON discovery directory (endpoint, description, mimeType, price, network, scheme, payTo) that the payer agent consumes before paying (plan §3–§4). Plus ENSv2 résumé discovery: `packages/record` ENSv2 writer/resolver built (commits `4922bc7`, `03cb055`), registration gated on Sepolia ETH + MockUSDC (checklist C5). | Plan + code | directory **IN-PROGRESS** (with H1) / ENSv2 BLOCKED-ON-CRED (C5) |
| H8 | Extra: "HTS tokens or custom fee schedules in settlement" | Settlement asset native HBAR (`asset: "0.0.0"`) via the x402 `exact` scheme; per-endpoint tinybar pricing table is the fee schedule. HTS-token settlement (testnet USDC `0.0.429274`) supported by the same `ExactHederaScheme` via the `asset` field — documented config flip, not default (USDC needs token association on both accounts) (plan §2, §4). | Plan §4 | **IN-PROGRESS** (with H1) |
| H9 | Extra: "verifiable payment audit trails on HCS" | After each successful settlement, the service submits a receipt memo to an HCS topic (`ConsensusSubmitMessage`, auto-create on first run): `{kind:"x402-receipt", tx, network, endpoint, price, agentId, ts}` — publicly verifiable on HashScan/DragonGlass; gated behind `HEDERA_SERVICE_KEY`, dry-run logs the memo (plan §3, §4). | Plan §4 | **IN-PROGRESS** (with H1) |
| H10 | Extra: "recurring/streamed payments via Scheduled Transactions" | `scripts/schedule-premium.ts` — a real scheduled recurring HBAR transfer; the REPAYD per-block premium maps to a `ScheduleCreate` recurring draw cadence (plan §4, README §Scheduled). | Plan §4 + script | **IN-PROGRESS** (with H1) |
| H11 | Extra: "multi-agent negotiation via A2A or ACP" | Not claimed — out of scope this pass, noted for roadmap (plan §4 row). | — | NOT PURSUED |

---

## The evidence spine (what every "live chain" claim above traces to)

- **Step-4 run (Arc testnet 5042002, 2026-09-11, blocks 61625041–61625840):** 14 tx hashes, every one re-fetched independently (status 0x1, correct `to`, correct sender) — full table with Arcscan links in [`local/step4-final-report.md`](../../local/step4-final-report.md) §1.
- **ERC-8004 mirror (readback from canonical registries):** agentId **894341**, owner `0x05499b0be3B9E9Db3Cc5124b2F682513D94133A6` (ops), wallet binding `0x9675b4D20d2ACFE55D00a02D55B9cdb57AEbD482`; validation score **25 (COVERED)**, responseHash `0x1af03bc6a70b6309bd5c9ec92c7d78c1024e0d69c9ea5ea60faf828c958ed3f0` (== on-chain `VerdictAccepted.digest`), tag `bulwark-verdict`; feedback value **−2500 @ 2dp**, tags `bulwark-verdict`/`covered`, not revoked.
- **Fresh-stack addresses:** `contracts/deployments/5042002.json` — guard `0xB30553e2…3b6E`, pool `0x35d78e52…72e4`, policyRegistry `0xcC34D028…748a`, verdicts `0x7B46f82B…C52f`, blocklist `0x19afa081…8322`, USDC `0xb95fe4d7…114B`.
- **Key commits:** ERC-8004 orchestrator `db98c7f`/`cad581a`; subgraph schema+mappings `bc5a400`/`1ec7979`/`4ef64e3`; pricing `981e977`/`a0b85eb`/`5282591`; record/ENSv2 `3212e7d`/`4922bc7`/`03cb055`; API bridge `ad325a7`/`56d5ee5`; deploy script `514f5eb`.
- **Graph deploy evidence:** [`docs/submission/graph-deploy.md`](graph-deploy.md) — verified step-by-step Studio deploy procedure (`graph auth` + IPFS upload confirmed live), COST $0 breakdown (100k queries/mo free tier, no card, no GRT), endpoint URL format, and the two-subgraph composition query pattern.