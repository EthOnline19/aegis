# REPAYD — ETHOnline 2026 Demo Video Scripts

Three scripts, second-by-second, ready to record the moment credentials land. Every hash, address, and ID below is **real** — from the verified Step-4 Arc-testnet run ([`local/step4-final-report.md`](../../local/step4-final-report.md), receipt `.wsl/step4-receipt.json`). Nothing needs to be re-run to record the ARC video; the GRAPH and HEDERA videos record after their credential unlocks (see [`credential-checklist.md`](credential-checklist.md)).

Recording notes: 1920×1080, terminal on left, browser (Arcscan / dashboard / Studio) on right. Narration pace ≈ 2.5 words/sec. Every on-screen CTA below is a deliberate beat — don't skip them.

## §ARC — "The Two-Gasp Run" (target 2:50, limit 3:00)

**Setup before recording:** terminal in `packages/demo`, browser open to `https://testnet.arcscan.app`. Dashboard (`packages/dashboard`, `/`) open in a second tab.

| Beat | Time | Screen | Narration | On-screen CTA |
|------|------|--------|-----------|---------------|
| ARC-1 | 0:00–0:12 | Title card: **REPAYD — deposit insurance for AI agents.** Sub: "Arc testnet · ERC-8004 identity · USDC settlement" | "Everyone is giving AI agents wallets. Nobody is insuring them. REPAYD is a protected wallet that holds what's suspicious, blocks what's dangerous, and pays back what slips through — same block. Watch one agent survive two attacks, live on Arc." | — |
| ARC-2 | 0:12–0:25 | Browser: `docs/submission/architecture.md` — the full-stack mermaid diagram, pointing at the agent↔GuardAccount↔USDC↔Circle Agent Stack flow | "The agent's wallet, its USDC spending, and its on-chain actions are wired through the Circle Agent Stack — the agent key `0x5c53…0F9f` is the wallet operator; every payment you're about to see settles in USDC on Arc. This is the whole machine on one diagram." | Arrow overlay: "Circle dev-controlled wallet (ARC-TESTNET) ↔ GuardAccount balance mirror" |
| ARC-3 | 0:25–0:45 | Terminal scrolling the Step-4 run log; Arcscan tab: tx `0xecd4321f…` then `0x84beb66a…` | "First, the boring part — proof the agent actually spends. Four autonomous USDC payroll payments: 180, 310, 90, and 220 dollars. The agent signed all four itself. This is an agent economy app with a working economy." | Highlight "Sent by 0x5c53…0F9f (agent key)" on each tx |
| ARC-4 | 0:45–1:15 | Arcscan tx `0xf795bf3f813248f4fd463adb91af6ca12c4208a51137de423f5b4d5e440934a1` — show `Held` event | "**Gasp one.** A hidden web-page instruction sends 150 dollars to a three-day-old wallet at 4 AM. The guard's decision logic — allowlist, recipient age, unusual hour, all real on-chain signals — routes it to a two-minute hold instead of executing. Nothing moves. The watcher's verdict: suspicious. Freeze." | Big overlay: `Held(holdId=1)` → "THE ATTACK NEVER SETTLED" |
| ARC-5 | 1:15–1:30 | Terminal: the `$900 → ATTACKER` attempted-override output; then Arcscan on the ATTACKER address showing balance 0 | "Then the same attacker tries a 900-dollar admin override. It never even reaches the network — the per-transaction cap trips a violation and the call reverts locally. Zero state change, zero fees. Balance: zero." | Overlay: "$900 blocked pre-broadcast — no tx exists, by design" |
| ARC-6 | 1:30–1:55 | Arcscan tx `0xed020f97235164333e416ae494c6b24462a6884e1aa2ba1a1898b85d07a299f6` — show `VerdictAccepted` event args; then Amara's address `0x9675…d482` USDC balance | "**Gasp two.** A look-alike address — edit distance one — slips past the allowlist. The guard holds it; the watcher checks the cryptographic alibi: the instruction came from outside, not the owner. Verdict: covered. And look at the block number — the 135-dollar payout lands in Amara's wallet **in the same block as the verdict**. Not next block. Same block." | Split overlay: verdict digest `0x1af0…d3f0` ↔ payout `135000000` — flash "SAME BLOCK" |
| ARC-7 | 1:55–2:15 | Dashboard tab: owner view (`/`) — policy, holds, verdicts feeding in live; alongside, `GET /v1/circle/agent-wallet` (or the dashboard card rendering it) showing the Circle dev-controlled wallet address + USDC balance mirror | "The owner sees all of it: the agent's ERC-8004 identity — agent 894341 — its policy, every hold, every verdict. And over here, the Agent Stack touchpoint: the agent's Circle wallet on Arc testnet, its USDC balance mirrored against the guard's on-chain balance, live from the Circle Wallets API." | Highlight "agentId 894341" + side-by-side balances "Circle wallet ‖ GuardAccount" |
| ARC-8 | 2:15–2:35 | Terminal: `cast call` readback of `validation.getValidationStatus` and `reputation.readFeedback` (the exact calls in report §2) | "And it's all provable. The validation registry scores the agent 25 — covered. The reputation registry records the payout: minus 25 dollars at two decimals, tagged covered. Anyone can re-run these reads — the agent now has an on-chain résumé." | Paste overlay: score `25` · feedback `−2500 @ 2dp` · tag `covered` |
| ARC-9 | 2:35–2:50 | Closing card: repo link, compliance-matrix link, "Arc bounty: Best Agentic Economy Application with Circle Agent Stack — $3,500" | "REPAYD: hold what's suspicious, prove who instructed what, pay what slips through — same block. Full compliance matrix and all fourteen transaction hashes are in the repo. This is the Arc bounty: Best Agentic Economy Application with the Circle Agent Stack." | QR/link: repo · `docs/submission/compliance-matrix.md` |

**If recording a fresh live run instead of playback:** run `packages/demo` against the Arc testnet stack (env keys in `.env`), then walk the same beats — the event shapes and readbacks are identical to the verified run.

---

## §GRAPH — "The Subgraph Is the Brain's Memory" (target 3:30, range 2–4 min)

**Prereq:** subgraph deployed to The Graph Studio (credential C1). Record only after deploy + sync.

| Beat | Time | Screen | Narration | On-screen CTA |
|------|------|--------|-----------|---------------|
| GRAPH-1 | 0:00–0:15 | Title card: **REPAYD Risk Subgraph — from scratch.** Sub: "36 events · 9 entities · live on The Graph" | "AI agents make decisions. Good decisions need memory. We built REPAYD's memory on The Graph — a risk subgraph indexing every policy, tier decision, hold, verdict, claim, and payout across six contracts. From scratch, no mocks." | — |
| GRAPH-2 | 0:15–0:40 | Browser: The Graph Studio — the deployed subgraph page, sync status "Synced", entity counts climbing | "Here it is, deployed and live through a Graph provider. Studio is indexing Arc testnet right now — this is not a local dataset, not a static file. Live chain, live indexer." | Highlight endpoint URL `https://api.studio.thegraph.com/query/…` |
| GRAPH-3 | 0:40–1:05 | Studio playground: query `agents { id streak premiumMultiplier claimCount }` — live result | "The subgraph does the arithmetic a raw indexer can't: clean-day streaks increment on every routine execution, reset on violations, and feed premium multipliers. Watch — agent 894341's streak, claims, and pricing state, all derived, all live." | Highlight `streak` and `premiumMultiplier` fields |
| GRAPH-4 | 1:05–1:35 | Studio playground: `verdicts` / `claims` query returning digest `0x1af0…d3f0`, payout 135000000 | "Every verdict from the run you saw in the Arc demo is queryable here — digest, outcome, payout, alibi. The subgraph is the single shared read replica for the whole protocol: the pricing engine and the résumé builder both read *this*, and neither runs its own indexer." | Highlight digest `0x1af03bc6…d3f0` matching the on-chain tx |
| GRAPH-5 | 1:35–2:05 | Terminal: the AI risk-posture consumer (from `docs/submission/graph-deploy.md`) — running a live GraphQL query against the Studio endpoint, then printing a computed Risk Posture | "Now the AI part — meaningful work, not pretty-printing. Our consumer queries the live subgraph and computes a Risk Posture: streak trajectory, breach recency, claim scars, blocklist strikes — weighted, labeled COMPUTED or VERIFIED, and turned into a premium decision." | Overlay: "LIVE QUERY → REASONING → DECISION" |
| GRAPH-6 | 2:05–2:30 | Terminal: pricing engine consuming the same live state — quote before vs. after the covered claim (0.72×/6mo vs post-claim next-term, from the integration tests) | "This is the decision: a fresh agent prices at 0.72× — the streak discount already earning. After a covered claim, the same live data reprices the next term. Reasoning over live subgraph data, end to end." | Overlay: `fresh: 0.72×/6mo` → `post-claim: repriced next term` |
| GRAPH-7 | 2:30–2:50 | Dashboard `/record` — the public résumé rendered from the same entities | "And it's a public good — anyone can query the same endpoint and audit any agent's history. Here's the résumé surface: INSURED, DRIVING, CLAIMS, ALIBI, BACKING, STATUS." | Highlight résumé section headers |
| GRAPH-8 | 2:50–3:15 | Studio playground (or GraphQL editor): the **composition query** — Risk Subgraph + ERC-8004/Agent0-standardized registry subgraph, joined on `agentId` 894341 | "Composition: we also index the canonical ERC-8004 registries — identity, validation, reputation — the Agent0 standardized schema. One query joins our bespoke risk entities with the standardized agent records on agentId 894341: two Graph products, one answer." | Overlay: "Bespoke Risk Subgraph ⨝ ERC-8004 standard schema" |
| GRAPH-9 | 3:15–3:30 | Closing card: repo, matrix links, "The Graph bounties: Best AI Tooling or AI Use Case (From Scratch) · Best Use of Composable or Standardized Graph Products — $5,000 each" | "From scratch, load-bearing, live, and composable. Full compliance matrices in the repo. These are The Graph's two bounties." | QR/link: repo · `docs/submission/compliance-matrix.md` |

---

## §HEDERA — "A Paid Conversation Between Agents" (target 4:30, limit 5:00)

**Prereq:** x402-gated service live on Hedera testnet via Blocky402 (credential C2). Record only after the service is up and one real paid request has succeeded once in rehearsal.

| Beat | Time | Screen | Narration | On-screen CTA |
|------|------|--------|-----------|---------------|
| HEDERA-1 | 0:00–0:15 | Title card: **REPAYD on Hedera — agents paying agents.** Sub: "x402 · Blocky402 facilitator · HCS receipts" | "What happens when an AI agent needs another agent's data — and has to pay for it, itself? We put REPAYD's Risk Posture API behind x402 on Hedera testnet, and let the guard agent buy what it needs. Real money, real requests." | — |
| HEDERA-2 | 0:15–0:40 | Browser: Blocky402 facilitator page; then the service URL — show the raw `402 Payment Required` response with the x402 payment demands | "Here's the service: a live x402-gated endpoint on Hedera testnet, settled through the Blocky402 facilitator. Ask without paying — you get 402, with the exact payment terms in the response. Machine-readable, machine-payable." | Highlight JSON: `402` + payment-demand fields + price per call |
| HEDERA-3 | 0:40–1:10 | Diagram card (from `docs/submission/hedera-x402-plan.md`): guard agent → 402 → pay HBAR via Blocky402 → Risk Posture → tier decision | "The flow: our guard agent — the same agent you saw survive two attacks on Arc — is about to send a payment. Before paying a new recipient it wants a risk posture. It calls the gated service, gets the 402, and pays per call." | Animated arrows over the 5-step payment flow |
| HEDERA-4 | 1:10–1:50 | Terminal: the consumer agent running — 402 received → x402 payment constructed + signed with the agent's own key → sent. Show HashScan (testnet.hashscan.io) opening on the **real payment transaction** | "**The real paid request.** Watch the terminal: 402 received. The agent constructs the x402 payment, signs it with its own key, sends it through Blocky402. There's the transaction on HashScan — real HBAR, real settlement, on Hedera testnet." | Highlight tx on HashScan: "operator ← agent · settled via Blocky402" |
| HEDERA-5 | 1:50–2:20 | Terminal: the `200 OK` arriving with the Risk Posture JSON; then the agent's tier decision printed using that posture | "Paid and answered — 200, with the Risk Posture: the recipient's streak, claims, and strikes from the live subgraph. And the agent doesn't just print it — it *uses* it: this recipient's posture feeds the tier decision for the payment it was about to make. Pay-per-call data metering, end to end." | Overlay: "402 → PAID → 200 → DECISION" |
| HEDERA-6 | 2:20–2:50 | Split screen: Arcscan on ERC-8004 identity readback (agentId 894341) ↔ HashScan on the Hedera payment | "Cross-chain identity: the agent paying on Hedera is the same ERC-8004 agent — identity 894341 — whose reputation you saw on Arc. One agent, one on-chain identity, two chains, paying for what it needs on both." | Overlay: "agentId 894341 — same agent, both chains" |
| HEDERA-7 | 2:50–3:20 | HashScan: the HCS topic + the receipt message for the paid request (txId + payment hash attested) | "Every paid request leaves a verifiable audit trail: the payment hash and request ID attested to an HCS topic — Hedera Consensus Service gives the receipt a consensus timestamp. Immutable, auditable, cheap." | Highlight HCS topic + consensus timestamp |
| HEDERA-8 | 3:20–3:45 | Terminal: scheduled-transaction batch settling accumulated per-call fees to the mutual pool | "And because per-call fees are tiny, REPAYD batches them: scheduled transactions aggregate a day of per-call payments into one settlement into the mutual pool. Recurring payments, the Hedera-native way." | Highlight Scheduled Transaction on HashScan |
| HEDERA-9 | 3:45–4:30 | Closing card: repo, plan doc, matrix; "Hedera bounty: AI & Agentic Payments on Hedera — $6,000 (up to 3 × $2,000)" + checklist of extras hit | "One real paid request, executed end to end. Agent identity on ERC-8004. HCS receipts. Scheduled-transaction settlement. Pay-per-call metering. Repo, setup, and the full payment flow are documented. This is the Hedera bounty: AI and Agentic Payments." | QR/link: repo · `docs/submission/hedera-x402-plan.md` · extras checklist |

---

### Cross-cutting recording checklist
- Every tx hash shown on screen must be one of the 14 from report §1 (Arc) or a fresh real tx (Hedera) — never a placeholder.
- Arcscan/HashScan links are in the compliance matrix and report §1; use them for the browser panes.
- Say "REPAYD" in every intro/outro (renamed from BULWARK — the run artifacts on-chain still say `bulwark-verdict` tags; that's the historical record, fine to show, narrate the rename if asked).
