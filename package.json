{
  "name": "aegis",
  "private": true,
  "version": "0.1.0",
  "description": "BULWARK — deposit insurance for AI agents. The seatbelt, the airbag, the black box, and the fleet contract.",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun run test:contracts && bun run test:js",
    "test:contracts": "cd contracts && forge test -vvv",
    "test:js": "bun workspaces foreach --all run test",
    "build:js": "bun build packages/dashboard/index.html",
    "lint": "bun workspaces foreach --all run lint"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/bun": "^1.1.0"
  }
}
