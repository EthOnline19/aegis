{
  "name": "aegis",
  "private": true,
  "version": "0.1.0",
  "description": "BULWARK \u2014 deposit insurance for AI agents. The seatbelt, the airbag, the black box, and the fleet contract.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun run test:contracts && bun run test:js",
    "test:contracts": "cd contracts && forge test",
    "test:js": "cd packages/engine && bunx vitest run && cd ../sdk && bunx vitest run && cd ../api && bunx vitest run",
    "lint": "cd packages/engine && bunx tsc --noEmit && cd ../sdk && bunx tsc --noEmit && cd ../api && bunx tsc --noEmit",
    "demo": "cd packages/demo && bun run src/demo.ts",
    "api": "cd packages/api && bun run start",
    "dashboard": "cd packages/dashboard && bun run start",
    "subgraph:build": "cd packages/subgraph && bunx graph codegen && bunx graph build"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/bun": "^1.1.0"
  }
}
